<resources>

    <style name="CustomTextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.ExposedDropdownMenu">
        <item name="boxStrokeColor">@android:color/white</item>
        <item name="boxBackgroundColor">@android:color/transparent</item>
        <item name="hintTextColor">@color/white</item>
        <item name="boxBackgroundMode">outline</item>
    </style>

    <style name="CustomTextInputLayoutHintStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:textSize">16sp</item> <!-- Change the hint text size here -->
    </style>
</resources>
